# python-crawler-learning 🕷️

My Python Web Crawler Learning Projects  
（Python 爬蟲學習紀錄）

---

## 💡 About This Repo | 專案介紹

This repository records my journey of learning **Python web crawling**.  
I use it to practice basic HTTP requests, HTML parsing, and data extraction.

這個專案用來紀錄我學習 **Python 爬蟲** 的過程，  
包含從最基礎的網頁請求、HTML 解析，到簡單的資料抓取與儲存。

---

## 🎯 Learning Goals | 學習目標

- Understand what HTTP, URL, Request, Response are  
- Use `requests` to fetch web pages  
- Use `BeautifulSoup` to parse HTML  
- Practice crawling simple websites (e.g. titles, articles, links)  
- Save data to CSV / JSON for later analysis

- 了解 HTTP、URL、請求與回應的概念  
- 使用 `requests` 抓取網頁內容  
- 使用 `BeautifulSoup` 解析 HTML 結構  
- 練習爬取簡單網站的標題、文章、連結等資料  
- 將資料存成 CSV / JSON，方便後續做資料分析

---

## 🛠 Environment | 開發環境

- Python 3.x
- PyCharm / VS Code
- Packages:
  - `requests`
  - `beautifulsoup4`
  - `pandas`（之後會用來處理表格資料）

Install dependencies:

```bash
pip install requests beautifulsoup4 pandas
